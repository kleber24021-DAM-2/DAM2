<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roomproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RoomApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomProject">
        <activity
            android:name=".ui.addheroactivity.AddHeroActivity"
            android:exported="false" />
        <activity
            android:name=".ui.updateheroactivity.UpdateHeroActivity"
            android:exported="false" />
        <activity
            android:name=".ui.herodetailactivity.HeroDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.herolistactivity.HeroListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>